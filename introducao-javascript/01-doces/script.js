//comentarios em javascript usamos duas barras
//criar variaveis
// as variaveis guardam um valor de um tipo diferente

const nomeDoce = "pudim";

const valor = R$ 3.0;

const disponivel = true;

console.log(nomeDoce, valor, disponivel);

document.getElementById(resultado).textContent

´${nomeDoce} custa R$ ${preco.toFixed(2)} - disponivel: ${disponivel ? "disponivel": "indisponivel"}´ ;
